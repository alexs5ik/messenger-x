target
mx_target
**/node_modules
web/dist
web/src/mxwasm
data
.git
**/*.log
# Windows-only toolchain pin — must NOT reach the Linux container build (it pins a
# stable-x86_64-pc-windows-gnu channel that fails on Linux). Cargo uses the image default.
rust-toolchain.toml
